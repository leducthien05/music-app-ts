extends ../../layout/default.pug

block content    
    if(roleAccount.permission.includes("singers_edit"))
        div(class="dashboard-form")
            form(
                class="form-add-song" 
                method="POST" 
                action=`${prefixAdmin}/singers/edit/${singer._id}?_method=PATCH`
                enctype="multipart/form-data"
            )
                
                // ===== THÔNG TIN CƠ BẢN =====
                .card
                    h3(class="card-title") Thông tin cơ bản

                    .form-grid
                        .form-group
                            label Tên ca sĩ/nghệ sĩ *
                            input(
                                type="text" 
                                name="nameSinger" 
                                value = singer.nameSinger
                                placeholder="VD: Sơn Tùng M-TP"
                            )
                    .form-group
                        label Mô tả *
                        textarea(
                            class="tinymce"
                            name="description" 
                            placeholder="Mô tả ngắn về bài hát..." 
                            rows="4"
                        ) !{singer.description}
                // ===== MEDIA =====
                .card
                    h3.card-title 🖼️ Hình ảnh 

                    .form-grid
                        .upload-box
                            label Hình ảnh bìa *
                            input(
                                type="file" 
                                name="avatar"
                            )
                            img(
                                src=singer.avatar
                            )

                // ===== CÀI ĐẶT =====
                .card
                    h3.card-title ⚙️ Cài đặt 

                    .form-grid
                        .form-group
                            label Trạng thái *
                            .radio-group
                                label
                                    input(
                                        type="radio" 
                                        name="status" 
                                        value="active" 
                                        checked = (singer.status == "active" ? true : false)
                                    )
                                    |  Hoạt động
                                label
                                    input(
                                        type="radio"
                                        name="status"
                                        value="inactive"
                                        checked = (singer.status == "inactive" ? true : false)
                                    )
                                    |  Ẩn

                // ===== ACTION =====
                .form-actions
                    button(
                        class="btn-reset" 
                        type="reset"
                    ) Làm mới
                    button(
                        class="btn-submit"
                        type="submit"
                    ) + Cập nhật ca sĩ